package <%= packageName %>;

import net.javacrumbs.shedlock.spring.annotation.EnableSchedulerLock;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.scheduling.annotation.EnableScheduling;

/**
 * <%= className %> — compliance event logger service (AFASA pattern).
 *
 * Generated by jsbb (java-springboot-bank-cli) v0.1.
 * Spring Boot <%= springBootVersion %>, Java <%= javaVersion %>, virtual threads enabled
 * via <code>spring.threads.virtual.enabled=true</code> in application.yml.
 */
@SpringBootApplication
@EnableScheduling
@EnableSchedulerLock(defaultLockAtMostFor = "PT15M")
public class <%= className %>Application {

    public static void main(String[] args) {
        SpringApplication.run(<%= className %>Application.class, args);
    }
}
